echo "🚀 Running CTO pre-push checks..."

# Run security scan
echo "🔒 Running security scan..."
node tools/security-mcp.js npm scan || {
  echo "⚠️  Security scan found issues, but continuing..."
}

# Run full test suite
echo "🧪 Running full test suite..."
npm test || {
  echo "❌ Tests failed. Push aborted."
  exit 1
}

# Run QA tests
echo "🎭 Running QA tests..."
node tools/qa-mcp.js playwright test || {
  echo "⚠️  QA tests failed, but continuing..."
}

# Optional: Run e2e tests (can be slow)
# echo "🧪 Running e2e tests..."
# npm run test:e2e || {
#   echo "⚠️  E2E tests failed, but continuing..."
# }

echo "✅ CTO pre-push checks passed!"
