echo "🚀 Running CTO pre-push checks..."

# Run security scan (non-blocking)
echo "🔒 Running security scan..."
node tools/security-mcp.js npm scan || {
  echo "⚠️  Security scan found issues, but continuing..."
}

# Run tests (non-blocking for now - can be made strict later)
echo "🧪 Running test suite..."
npm test || {
  echo "⚠️  Some tests failed, but continuing with push..."
  echo "💡 To enforce strict testing, update .husky/pre-push"
}

echo "✅ Pre-push checks complete - proceeding with push"
